<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="e.rahmanapyrr.gift_app">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".ParseApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LogIn.LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LogIn.RegisterBirthdayActivity" />
        <activity android:name=".LogIn.RegisterNameActivity" />
        <activity android:name=".ProductActivity.LinkActivity" />
        <activity android:name=".Calendar.CalendarActivity"/>
        <activity android:name=".LogIn.RegisterActivity" />
        <activity android:name=".Profile.ProfileActivity" />
        <activity android:name=".Friends.AddFriends" />
        <activity android:name=".Friends.CurrentUserFriends" />
        <activity android:name=".Profile.PersonalPageActivity" android:exported="true"/>
        <activity android:name=".Config.PaymentDetails" />
        <activity android:name=".Paypal.BuyingActivity" />


        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="e.rahmanapyrr.gift_app"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/paths" />
        </provider>


    </application>

</manifest>